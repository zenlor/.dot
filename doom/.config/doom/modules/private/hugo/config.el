;;; private/hugo/config.el -*- lexical-binding: t; -*-

(after! ox
        (use-package! ox-hugo))

; (def-package! ox-hugo
;   :ensure t
;   :after 'ox)

 ; (map! :map org-mode-map
 ;      (:prefix ("u" . "hugo")))
